test:
  errors:
    messages:
      before: "wakka foobars for %{restriction}"
  validates_timeliness:
    error_value_formats:
      date: '%d/%m/%Y'
      time: '%H:%M:%S'
      datetime: '%d/%m/%Y %H:%M:%S'
